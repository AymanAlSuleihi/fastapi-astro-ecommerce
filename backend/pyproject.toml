[project]
name = "fastapi-astro-ecommerce-backend"
version = "0.1.0"
description = "Ecommerce backend - FastAPI + PostgreSQL"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.138.1",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.13",
    "pydantic-settings>=2.4",
    "pyjwt>=2.9",
    "passlib[bcrypt]>=1.7",
    "httpx>=0.27",
    "bcrypt<5.0",
    "resend>=2.0",
    "jinja2>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest-asyncio>=0.24",
    "ruff>=0.6",
]

[tool.fastapi]
entrypoint = "src.main:app"

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "C4"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=src --cov-report=term-missing"

[dependency-groups]
dev = [
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.20",
    "ty>=0.0.55",
]
